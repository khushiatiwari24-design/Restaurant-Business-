# PostgreSQL
POSTGRES_USER=dilyum
POSTGRES_PASSWORD=change-me
POSTGRES_DB=dilyum
DATABASE_URL=postgresql://postgres:change-me@localhost:5432/Restaurant_DB?schema=public

# NestJS
PORT=3001
FRONTEND_ORIGIN=http://localhost:3000

# JWT
JWT_SECRET=replace-with-a-long-random-secret
JWT_EXPIRES_IN=12h

# Super Admin seed (local development only — never commit real secrets to shared repos)
SUPER_ADMIN_EMAIL=vvinit594@gmail.com
SUPER_ADMIN_PASSWORD=Admin@123
SUPER_ADMIN_NAME=Platform Super Admin
